# Memory: index.md
Updated: just now

# Project Memory

## Core
Homepage uses a 12-bucket vertical structure. Always reference work by bucket number (e.g. "Bucket 2 = Hero").
NEVER use the browser/sandbox automation tool. User has explicitly forbidden it. Debug via code reading, console logs, network logs, and dev-server logs only.

## Memories
- [Page Map (12 Buckets)](mem://features/page-map) — Full top-to-bottom homepage structure with film names and footer layers
